import 'package:flutter/material.dart';

class DireccionesPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    //No se usa el Scafol Porque esta parte cambiaria solo la vista Mientras el header y footer quedan estaticos
    return Center(
      child: Text('Direcciones Page'),
    );
  }
}
